# No paths here anymore
batch_size: 512
num_workers: 14
pin_memory: true
adj_path: ${oc.env:PVC_ADJ_PATH, ${hydra:runtime.cwd}/resources/adjacency_matrix_tensor.pt}

# (optional) label scaling
target_unit: s        # or "ms" or "samples"
variable_length: true         # if false, you can still force-resample to 430 in the dataset
pad_value: 0.0
max_len: null
